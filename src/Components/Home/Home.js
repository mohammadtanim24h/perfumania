import React, { useState } from 'react';
import usePerfumes from '../../hooks/usePerfumes';
import './Home.css';

const Home = () => {
    const [perfumes] = usePerfumes();
    return (
        <div>
            
        </div>
    );
};

export default Home;